1. to create all spatie things at once:
   php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"
2. run if there are file ownership problems on a new server:sudo chmod -R 777 .
